export type JobData = {
  fullname: string;
  email: string;
  date: string;
  body: string;
};

export type ResponseProps = {
  firstname: string;
  lastname: string;
  email: string;
  date: string;
  answers: string[];
};
